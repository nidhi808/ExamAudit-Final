def ask_exam_bot(question):
    # Azure OpenAI hook ready
    return (
        "ExamAudit uses statistical deviation (Z-score) to detect "
        "unfair grading. Students are flagged only when scores "
        "significantly differ from the class average."
    )
